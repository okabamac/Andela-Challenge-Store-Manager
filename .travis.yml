language: node_js

node_js:
  - "7"

notifications:
  email: true

script: npm start

cache:
  directories:
    - "node_modules"

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: gh-pages
  local_dir: "output"
